########################## LEONARDO ###############################

         # spring.datasource.url = jdbc:mysql://localhost:3306/testSpring?useSSL=false&serverTimezone=UTC
         # spring.datasource.username= root
         # spring.datasource.password=

         # spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.MySQL5InnoDBDialect
         # spring.jpa.hibernate.ddl-auto = create

         # logging.level.org.hibernate.SQL=DEBUG
         # logging.level.org.hibernate.type=TRACE

         # jwt.header: Authorization
         # jwt.secret: mySecret
         # jwt.expiration: 7200




########################## MARCO ###############################

    spring.jpa.hibernate.ddl-auto=create
    spring.datasource.url=jdbc:mysql://localhost:3306/prova?useSSL=false&requireSSL=false&useLegacyDatetimeCode=false&serverTimezone=UTC
    #jdbc:mysql://provaserversql.mysql.database.azure.com:3306/anavis?useSSL=false
    #&requireSSL=false&useLegacyDatetimeCode=false&serverTimezone=UTC
    spring.datasource.username=root
    #myadmin@provaserversql
    spring.datasource.password=
    spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect
    spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
    
    jwt.header: Authorization
    jwt.secret: mySecret
    jwt.expiration: 7200

   #   list.of.auth.for.Donatore = donare,profilo
   #   list.of.auth.for.SedeAvis = donare,handlerDate,profilo
   #   list.of.auth.for.CentroTrasf = requestEmerg,profilo


